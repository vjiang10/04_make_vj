#include "euler.h"
#include <stdio.h>

int main() {
    printf("the answer to problem 1: %d\n", p1());
    printf("the answer to problem 2: %d\n", p2());
    printf("the answer to problem 5: %d\n", p5());
    printf("the answer to problem 6: %d\n", p6());

    return 0;
}